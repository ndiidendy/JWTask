‎README# JWTask (Менеджер задач)

Это простое приложение-менеджер задач (To-Do list), созданное для [укажите цель, например: "учебного курса" или "портфолио"].

Приложение использует Firebase для бэкенда (хранение данных, аутентификация).

---

## 💻 Использованные технологии

* **Фронтенд:** [Укажите ваш фреймворк, например: React, Next.js, Angular, или "чистый" HTML/CSS/JS]
* **Бэкенд:** Firebase
* **База данных:** Firebase Realtime Database
* **Хостинг:** Firebase App Hosting

---

## 🚀 Как запустить проект локально

1.  **Клонируйте репозиторий:**
    ```bash
    git clone [https://github.com/ndiidendy/JWTask.git](https://github.com/ndiidendy/JWTask.git)
    cd JWTask
    ```

2.  **Установите зависимости:**
    (Этот шаг как раз требует наличия `package.json` в проекте)
    ```bash
    npm install
    ```

3.  **Настройте Firebase:**
    * Создайте файл `.env.local` (или `.env`) в корне проекта.
    * Добавьте в него ваши ключи из консоли Firebase (те, что видны в логах сборки, но их нельзя публиковать в Git!).
    ```ini
    # Пример для React/Next.js
    NEXT_PUBLIC_FIREBASE_API_KEY="AIzaSy..."
    NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN="taskapprez2..."
    NEXT_PUBLIC_FIREBASE_DATABASE_URL="https://taskapprez2..."
    NEXT_PUBLIC_FIREBASE_PROJECT_ID="taskapprez2..."
    NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET="taskapprez2..."
    NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID="7235..."
    NEXT_PUBLIC_FIREBASE_APP_ID="1:7235..."
    ```

4.  **Запустите приложение:**
    ```bash
    # Укажите вашу команду для запуска
    npm run dev
    # или
    npm start
    ```
